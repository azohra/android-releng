package co.ld.codechallenge.repository

import co.ld.codechallenge.data.RepositoryManager
import org.junit.Before
import org.junit.Test
import javax.inject.Inject


class SearchRepositoryTest {

    lateinit var subject: SearchRepository

    lateinit var repositoryManager: RepositoryManager

    @Before
    fun setUp() {
//        val component: TestComponent = DaggerTestComponent.builder()
//                .appModule(TestModule())
//                .build()
//
//        component.inject(this)
//
//        subject = SearchRepository()
    }

    @Test
    fun test1() {
//        `when`(repositoryManager.execute(any<SearchRepository>()))
//                .thenReturn(Single(emptyList()))
//        subject.getData("hello")
//
//        verify(repositoryManager).execute(subject)
    }
}